Open Git Bash, then run:
git --version

If you see version info, Git is installed correctly.
Now configure Git:
git config --global user.name "Your Name"
git config --global user.email "youremail@example.com"

To verify:
git config --list
